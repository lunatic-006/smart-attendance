{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "uvicorn backend.main_api:app --host 0.0.0.0 --port $PORT",
    "restartPolicy": "on_failure",
    "numReplicas": 1
  }
}
